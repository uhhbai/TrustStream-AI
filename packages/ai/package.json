{
  "name": "@truststream/ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@truststream/shared": "workspace:*",
    "openai": "^4.76.0"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
